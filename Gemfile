source 'https://rubygems.org'

gem 'rails', '~> 5.0.0'

if Dir.exists?(File.expand_path('~/workspace/web-console'))
	gem 'web-console', :path => '~/workspace/web-console'
else
	gem 'web-console', :github => 'sh19910711/web-console', :ref => 'wip/0037/1'
end
